# Plan

此目录存放实施计划。

推荐模板：

```markdown
# 功能名称实施计划

## 关联文档

## 范围

## 里程碑

## 任务

| 任务 | 状态 | 依赖 | 验证方式 |
|---|---|---|---|
| 示例 | pending | 无 | 单元测试 |

## 风险与缓解措施

## 发布与回滚
```

状态只使用 `pending`、`in_progress`、`completed`、`blocked`，同一时间只能有一个主要任务处于 `in_progress`。
